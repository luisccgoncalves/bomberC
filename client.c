#include "structs.h"
#include <stdio.h>
#include <stdlib.h>


int main(int argc, char** argv) {

    user newUser;
    running=1;

    printf("bomberC\nPlease login.\n");
    while(running){
        printf("user:");
        scanf(" %[^\n]s");
    }

    return (EXIT_SUCCESS);
}
